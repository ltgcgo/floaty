{
	order request_id before header
}

http://:8040 {
	log {
		level INFO
	}
	request_id 32
	header X-Floaty {http.request_id}
	header X-Time {time.now.http}
	respond "Hello! The current Floaty ID is: {{placeholder \"http.request_id\"}}."
	templates
}
